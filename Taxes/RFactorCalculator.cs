namespace PJxCLTCalculator {
    public class RFactorCalculator {
        public double calculateRFactor(double grossIncome, double ownerSalary) {
            return ownerSalary / grossIncome;
        }
    }
}